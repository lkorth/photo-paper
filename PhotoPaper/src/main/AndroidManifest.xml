<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lukekorth.android_500px">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
        android:name=".WallpaperApplication">

        <meta-data android:name="AA_DB_NAME" android:value="500px.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="3" />
        <meta-data android:name="AA_MODELS"
            android:value="com.lukekorth.android_500px.models.Photos, com.lukekorth.android_500px.models.User" />

        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchActivity" android:label="@string/search" />
        <activity android:name=".HistoryActivity" android:label="@string/history" />
        <activity android:name=".ViewPhotoActivity" android:theme="@style/FullScreenPhotoTheme" />

        <service android:name=".services.UserInfoIntentService" />
        <service android:name=".services.ApiService" />
        <service android:name=".services.PhotoCacheIntentService" />
        <service android:name=".services.WallpaperService" />
        <service android:name=".services.ClearCacheIntentService" />

        <receiver android:name=".receivers.UserPresentReceiver">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
        <receiver android:name=".receivers.WifiReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
